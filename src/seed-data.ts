// Seed configuration module - initialization handled dynamically via authenticated session
export async function seedInitialData() {
  // No-op for unauthenticated startup to prevent permission-denied errors
}
